# Changelog

## 1.3.8

- 修复：正计时暂停→继续→再暂停时，第二段历史记录按区间增量计算
- 修复：暂停后时间轴折叠态空白；删除单条历史记录后折叠态空白
- 修复：日常事务按钮启动的正计时重置后不再切回倒计时模式
- 修复：历史编辑模式点击空白补录，支持日常按钮的休息模式（休息正计时/休息倒计时）
- 修复：历史编辑模式更换关联任务后，记录计时模式同步为按钮计时模式

## 1.3.6

- 修复：清除关联状态后，日常按钮生成的图标未还原
- 修复：番茄计时结束后弹窗按钮无法点击，无反应
- 修复：增加清除定时器逻辑，确保弹窗点击选项后正确清除系统提醒
- 修复：暂停再开始会生成一条1分钟计时记录
- 修复：暂停后时间轴高亮缓存未清除问题

## 1.3.3

- 优化：每日历史记录的任务合计调整为本日合计，只统计相同任务当日的时间记录
- 优化：开启小时格式H:MM:SS格式后会同步应用至历史记录显示
- 优化：休眠后折叠状态时间轴残留未刷新问题
- 修复：删除单条历史记录后滚动位置丢失问题
- 修复：在保存历史记录前进行去重检查，防止更新重载代码后同一个时间段被重复保存多次
- 修复：时间轴暂停后颜色未正确刷新

## 1.3.2

- 优化：降低时间轴呼吸动画开销
- 优化：添加任务块图标、时间轴刷新以及历史记录缓存失效逻辑，确保休眠或其他情况下另一端产生数据后能完全刷新
- 优化：暂停或停止计时均会触发思源同步，提高状态同步稳定性
- 修复：暂停功能失效问题
- 修复：正在计时中的高亮时间轴块无悬浮窗问题
- 修复：休息番茄计时无呼吸效果问题
- 修复：休息模式按钮计时时间轴块名称无关联问题

## 1.3.1

- 修复：电脑长时间休眠/跨天恢复后，刷新可能只更新部分信息（如时间/关联项目），但计时按钮图标、日常事务按钮运行态高亮、时间轴“进行中”高亮与历史数据未同步更新，需要重载插件才正常
- 优化：唤醒时自动触发一次界面强制刷新；长间隔/跨天时清理文件读取缓存并重载设置，使恢复后的状态更新更接近“插件重启”
- 优化：若历史对话框在休眠前已打开，恢复后自动刷新对话框内容（仍保持“有数据才显示今天/昨天/前天”）

## 1.3.0

- 新增：历史记录编辑功能：在“查看历史”的当天/某天页面右上角开启“编辑模式”
- 新增：编辑模式下支持点击时间轴空白处补录、拖动片段调整起止时间
- 新增：编辑模式的日常事务选择支持按分组展示（含空白选项）并支持图片图标（img: 路径）
- 优化：关闭/卸载时统一托管并清理定时器，减少残留任务导致的性能占用
- 优化：数据库菜单监听收敛到 #commonMenu，避免对 document.body 进行 attributes 全局监听
- 修复：移动端 inject 触发频繁时可能重复初始化，导致监听/Observer 叠加
- 调整：beforeunload 同步改为非阻塞 best-effort，减少退出/刷新卡顿

## 1.2.7

- 新增：主设置支持自定义“分心提示文本”（支持 {count} 占位符）
- 优化：分心提示改为居中 Toast 并延长至 3 秒
- 修复：外观设置“呼吸速度”切换后选项高亮边框/背景未更新
- 修复：跨天计时在时间轴上按日期拆分显示；结束后上一天片段不丢失

## 1.2.6

- 修复：休眠唤醒后自动触发一次强制同步与界面刷新，避免跨端切换不及时
- 文案：历史记录的统计页改名为“专注统计”

## 1.2.5

- 调整：正计时最长时间限制提升至 16 小时
- 修复：多端同步/重置后偶发显示跳到最大正计时的问题
- 新增：移动端时间轴支持下滑手势折叠
- 新增：时间轴高亮块圆角设置（2px）
- 修复：番茄开始后时间轴“计划中/已专注”两段高亮不显示（兼容秒/毫秒时间戳）
- 修复：从日常事务按钮/任务块菜单/数据库菜单开始计时，时间轴高亮未使用按钮自定义颜色的问题
- 优化：时间轴进行中渲染稳定性（NaN 防护）
- 优化：等待菜单元素的轮询增加超时；移动端面包屑按钮监听节流，减少潜在高频 DOM 查询

## 1.2.3

- 优化：界面文案优化

## 1.2.2

- 新增：日常事务按钮开始计时后，底栏与移动端悬浮条图标使用按钮图标（emoji）

## 1.2.1

- 修复：日常事务按钮开始的正计时/休息正计时，时间轴“进行中”区间颜色未使用按钮颜色的问题
- 新增：时间轴高亮片段鼠标悬停指示效果（提亮/上浮/描边阴影）
- 新增：时间轴高亮“玻璃立体效果”，支持开关与强度调节（默认开启）
- 优化：时间轴设置弹窗支持滚动，内容较多时不再溢出
- 文案：将“日常事务按钮高亮同步到时间轴”改名为“日常事务按钮颜色同步到时间轴”

## 1.2.0

- 新增：日常事务按钮（Routine Buttons），支持一键开始番茄/正计时、分组/图标/颜色配置，并可同步高亮到时间轴
- 新增：历史记录增加日常统计面板（今日/本周/本月等汇总与目标进度展示）
- 新增：分心记录（计时中右键“😵 记录分心”，可选开启每次分心 +1 分钟延长番茄）
- 新增：桌面端“系统弹窗未确认持续提醒（每60秒）”开关
- 优化：时间轴刻度线更细；刻度默认位置调整为“下方”；新增“仅显示小时（隐藏分钟）”开关（默认开启）
- 新增：时间轴刻度字体大小拆分为桌面端/移动端独立设置（默认：桌面 10px、移动 8px）
- 修复：时间轴展开/显示时刻度线无需等待 1 秒，立即出现
- 调整：移除“刻度时间垂直（右侧）”排布模式
- 文档：中文 README 增加 1.2.0 更新内容与完整使用指南

## 1.1.0

- 修复历史记录界面异常刷新问题

## 1.0.9

- 修复历史记录界面进入后不刷新问题

## 1.0.8

- 重新补充版本更新1.0.8

## 1.0.7

- 删除无用目录和图标

## 1.0.6

- 发布包不再包含 CHANGELOG.md 与 LICENSE

## 1.0.5

- 启动时不再写入配置文件/不再 mkdir；配置缺失时直接使用默认配置
- 减少重复的 getFile 请求（增加读取缓存）
- 历史记录过滤不再自动回写文件

## 1.0.4

- 修复卸载时删除音频目录路径导致的 400 报错

## 1.0.3

- 修复卸载清理接口调用导致的 400 报错

## 1.0.2

- 卸载插件时删除配置与音频文件，仅保留历史记录

## 1.0.1

- 修复存储目录迁移/选择逻辑在不同 API 返回形态下的不兼容问题
- 全新安装时确保在 petal 目录创建默认配置文件
- 修复音频设置保存不固化的问题（写入校验与 localStorage 兜底）
- 音频目录不再自动迁移：旧目录下已有音频文件则继续使用旧目录
- 修复主设置页“番茄钟时长/休息间隔”输入框溢出

## 1.0.0

- 初始发布
